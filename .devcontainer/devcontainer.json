{
    "name": "Workers",
    "dockerComposeFile": "docker-compose.yml",
    "service": "workers",
    "workspaceFolder": "/py-workers",
    "shutdownAction": "stopCompose",
    "postCreateCommand": "poetry shell",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
            ]
        }
    }
}
